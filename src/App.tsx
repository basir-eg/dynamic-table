import {DynamicDataTable} from "@/components/table/DynamicDataTable.tsx";

export const App = () => {

  return (
      <div className={"h-full w-full"}>
        <DynamicDataTable/>
      </div>
  )
}